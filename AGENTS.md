# TIMERECORD 项目工作说明

开始处理本项目的任何任务前，必须先完整阅读 docs/WORK_SESSION_REPORT.md。

工作时还应遵守以下规则：

- 保留用户已有修改，修改前确认当前版本和相关权限范围。
- 涉及老板、园长、员工、孩子等角色时，同时检查前端显示、后端接口、项目隔离和数据归属。
- 修改 webapp.py 后至少运行 Python 语法检查和发布就绪检查。
- 部署脚本显示成功不等于线上版本已经生效；必须核对实际监听 8000 端口的进程及其 systemd 服务。
- 恢复备份时不要只覆盖单个文件，应先确认该版本包含的功能和数据库结构，避免遗失后续修改。

